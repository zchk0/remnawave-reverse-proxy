---
title: Node only
description: Instructions for installing the RemnaWave node on your server.
---

import { Steps } from '@astrojs/starlight/components';
import { LinkButton } from '@astrojs/starlight/components';

## Running the script

:::danger[Warning]
Script must be run as **root**, and also on the server where the Remnawave node will be installed.
:::

Enter the command on your server:

```bash
bash <(curl -Ls https://raw.githubusercontent.com/zchk0/remnawave-reverse-proxy/refs/heads/main/install_remnawave.sh)
```

## Installing the node

<Steps>

1. Select `Install Remnawave components`

    ![Main menu RU](https://raw.githubusercontent.com/zchk0/remnawave-reverse-proxy/refs/heads/main/docs/src/assets/en/main_menu.webp)

2. Then select `Install only the node (tcp)` or `Install only the node (xhttp)`.

   For `xhttp` mode, the panel side must use a single `VLESS + XHTTP + REALITY` inbound with a local `target` of `127.0.0.1:8001`.

    ![Install node RU](https://raw.githubusercontent.com/zchk0/remnawave-reverse-proxy/refs/heads/main/docs/src/assets/en/install_node.webp)

3. Log in to the control panel, go to the `Nodes` → `Management` section, select the desired node, and find the `Secret Key` (SECRET_KEY) field. Copy the certificate using the copy icon.

    ![SSL_CERT RU](https://raw.githubusercontent.com/zchk0/remnawave-reverse-proxy/refs/heads/main/docs/src/assets/en/ssl_cert.webp)

4. Script will ask you for the selfsteal domain that you specified during the panel installation or through the [`Add node to panel`](https://wiki.egam.es/installation/add-node/) section, the panel's IP address, and the SSL certificate from the panel.

    :::danger[Warning]
    Don't forget to add a DNS record for your domain.
    :::

    ![Menu node end RU](https://raw.githubusercontent.com/zchk0/remnawave-reverse-proxy/refs/heads/main/docs/src/assets/en/menu_node_end.webp)

5. Then the script will start the node and connect it to the panel.

    ![Menu node end2 RU](https://raw.githubusercontent.com/zchk0/remnawave-reverse-proxy/refs/heads/main/docs/src/assets/en/menu_node_end2.webp)

</Steps>

## Updating the script

Make sure to update the script before running things in it. You can do this by selecting `Check for updates script` in the script menu or by viewing the GitHub repository for updates.

<div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '1rem', textAlign: 'center' }}>
    <LinkButton
        href="https://github.com/zchk0/remnawave-reverse-proxy/releases/latest"
        icon="github"
        iconPlacement="start"
    >
        View latest release
    </LinkButton>
</div>
